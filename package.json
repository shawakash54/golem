{
  "name": "golem",
  "version": "1.0.0",
  "description": "CRON to index geoname IN.zip data to ES",
  "author": "Akash Shaw",
  "license": "MIT",
  "dependencies": {
    "serverless-plugin-scripts": "^1.0.2"
  },
  "devDependencies": {
    "serverless-offline": "^5.11.0",
    "serverless-offline-scheduler": "^0.3.8"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install -g serverless && serverless datasetdownload",
    "start": "sls offline"
  }
}
